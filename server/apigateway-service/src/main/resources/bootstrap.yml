spring:
  cloud:
    config:
      uri:
        - http://localhost:8888
        - http://config-green:8888
        - http://config-blue:8888
      name: apigateway-service
management:
  endpoints:
    web:
      exposure:
        include: refresh, health, beans,httptrace, busrefresh